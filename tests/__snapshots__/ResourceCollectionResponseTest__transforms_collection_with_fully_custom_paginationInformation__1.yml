description: 'Paginated set of `UserResource`'
content:
    application/json: { schema: { type: object, properties: { data: { $ref: '#/components/schemas/UserCollection_Six' }, links: { type: object, properties: { first: { type: [string, 'null'] }, last: { type: [string, 'null'] }, prev: { type: [string, 'null'] }, next: { type: [string, 'null'] } }, required: [first, last, prev, next] }, meta: { type: object, properties: { currentPage: { type: integer, minimum: 1 }, lastPage: { type: integer, minimum: 1 }, from: { type: [integer, 'null'], minimum: 1 }, to: { type: [integer, 'null'], minimum: 1 }, total: { type: integer, description: 'Total number of items being paginated.', minimum: 0 }, pageSize: { type: integer, description: 'Number of items shown per page.', minimum: 0 }, path: { type: [string, 'null'] } }, required: [currentPage, lastPage, from, to, total, pageSize, path] } }, required: [data, links, meta] } }
