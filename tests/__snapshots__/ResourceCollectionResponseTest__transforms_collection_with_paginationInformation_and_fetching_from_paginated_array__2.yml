description: 'Paginated set of `UserResource`'
content:
    application/json: { schema: { type: object, properties: { data: { $ref: '#/components/schemas/UserCollection_Seven' }, page: { type: integer, minimum: 1 }, totalPages: { type: integer, minimum: 1 } }, required: [data, page, totalPages] } }
