type: object
properties:
    foo: { type: integer, const: 1 }
    wait: { type: object, properties: { one: { type: integer, const: 1 }, bar: { type: string, const: foo }, kek: { type: object, properties: { bar: { type: string, const: foo } }, required: [bar] } }, required: [one, bar, kek] }
    hey: { type: string, const: ho }
    bar: { type: string, const: foo }
required:
    - foo
    - wait
    - bar
