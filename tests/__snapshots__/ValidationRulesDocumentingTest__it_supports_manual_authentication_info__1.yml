openapi: 3.1.0
info:
    title: Laravel
    version: 0.0.1
servers:
    - { url: 'http://localhost/api', description: 'Live Server' }
security:
    - { apiKey: {  } }
paths:
    /test: { get: { tags: [WithoutSecurity], responses: { 200: { description: '', content: { application/json: { schema: { type: string } } } } }, security: [{  }] } }
components:
    securitySchemes: { apiKey: { type: apiKey, in: query, name: api_token } }
